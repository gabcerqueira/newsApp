import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const ReadListContainer = (props: Props) => {
  return (
    <View>
      <Text>ReadListContainer</Text>
    </View>
  );
};

export default ReadListContainer;

const styles = StyleSheet.create({});
